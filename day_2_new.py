def read_input_file(file_path):
    # takes file path and reads file line by line
    parse_input_line(line)

def parse_input_line(line):
    # extract game id
    game_id = extract_game_id(line)
    #parse inputs to set/list
    line_list = create_subsets_from_input_line(line)
def extract_game_id(line):
    # extract game id and return

def create_subsets_from_input_line(line):
    # seperate input line into list of values
    handle_each_subset(subset)

def handle_each_subset(subset):
    # store colors and count
